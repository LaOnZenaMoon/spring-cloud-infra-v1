server:
  port: 9082

spring:
  application:
    name: hystirx-turbine

  profiles:
    active: local

logging:
  level:
    root: info

turbine.appConfig: external-gateway
turbine.clusterNameExpression: new String("default")